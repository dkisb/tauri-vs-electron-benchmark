{
  "name": "tauri-electron-bench",
  "version": "1.0.0",
  "description": "Benchmark comparing Tauri and Electron",
  "type": "module",
  "scripts": {
    "setup": "bun run setup:electron && bun run setup:tauri && bun run setup:icons",
    "setup:electron": "cd electron-app && bun install",
    "setup:tauri": "cd tauri-app && bun install",
    "setup:icons": "bun run scripts/gen-icons.ts",
    "build": "bun run build:electron && bun run build:tauri",
    "build:electron": "cd electron-app && bun run build",
    "build:tauri": "cd tauri-app && bun run build",
    "bench": "bun run bench/index.ts",
    "clean": "rimraf electron-app/dist electron-app/out tauri-app/src-tauri/target results/*.json results/*.md"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "rimraf": "^5.0.0"
  }
}
